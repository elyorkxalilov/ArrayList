package Lesson_29.I.Hemwork_1;

public class RecerCar implements RecersDriver {
    @Override
    public void recingDriver(String recerName, int recerAge) {
        System.out.println(recerName + " men " + recerAge + " yoshdaman, poygachiman!");
    }
}
